// 'use strict'

// import mongoose from 'mongoose'

// // Vars and consts
// const domain = 'parquesgir.test'
// const portDB = 27017


// mongoose.connect(`mongodb://${domain}:${port}`, (err, res) => {
//   if (err) {
//     return console.log(`Error al conectar a mongodb: ${err}`)
//   }

//   console.log('¡Conexión a la base de datos establecida!')
//   app.listen(port, () => console.log(`running in ${domain}:${port}`))
// })
